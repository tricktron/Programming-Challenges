#include <stdio.h>
#include <string.h>
//
// Created by Thibault Gagnaux on 05.12.17.
//
#define MAX_SIZE 9
int main() {
  for (;;) {
    int size, number;
    scanf(" %d %d", &size, &number);
    int n = (int) strlen(number);
    if (size == 0 && number == 0) break;
    for (int i = 0; i < 2 * size + 3; i++) {
      for (int j = 0; j < )
    }

  }
}
